# Core data handling
pandas>=2.0
numpy>=1.24

# Parquet support (required for read_parquet / to_parquet)
pyarrow>=14.0

# Machine Learning
scikit-learn>=1.4

# Plotting & visualization
matplotlib>=3.8

# Optional models (safe to keep: code handles absence)
xgboost>=2.0

# Optional explainability (only used if --shap)
shap>=0.44
